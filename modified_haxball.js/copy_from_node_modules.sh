#!/bin/bash

SCRIPT_DIR="$(dirname "$(realpath "$0")")"
HAXBALL_JS="$SCRIPT_DIR/../node_modules/haxball.js/src/index.js"
TYPES_JS="$SCRIPT_DIR/../node_modules/@types/haxball-headless-browser/index.d.ts"

cp "$HAXBALL_JS" "$SCRIPT_DIR/"
cp "$TYPES_JS" "$SCRIPT_DIR/"
